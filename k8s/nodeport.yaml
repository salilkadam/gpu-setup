apiVersion: v1
kind: Service
metadata:
  name: ai-routing-api-np
  namespace: ai-infrastructure
  labels:
    app: ai-routing-api
spec:
  type: NodePort
  ports:
  - port: 8001
    targetPort: 8001
    nodePort: 30001
    protocol: TCP
    name: http
  selector:
    app: ai-routing-api
---
apiVersion: v1
kind: Service
metadata:
  name: ai-stt-service-np
  namespace: ai-infrastructure
  labels:
    app: ai-stt-service
spec:
  type: NodePort
  ports:
  - port: 8002
    targetPort: 8002
    nodePort: 30002
    protocol: TCP
    name: http
  selector:
    app: ai-stt-service
---
apiVersion: v1
kind: Service
metadata:
  name: ai-tts-service-np
  namespace: ai-infrastructure
  labels:
    app: ai-tts-service
spec:
  type: NodePort
  ports:
  - port: 8003
    targetPort: 8003
    nodePort: 30003
    protocol: TCP
    name: http
  selector:
    app: ai-tts-service
---
apiVersion: v1
kind: Service
metadata:
  name: ai-vllm-service-np
  namespace: ai-infrastructure
  labels:
    app: ai-vllm-service
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30000
    protocol: TCP
    name: http
  selector:
    app: ai-vllm-service
